---
description: Continue Goal Mode from blocking review findings and run the next repair cycle.
agent: goal
---

## What this command gives you

`/goal-repair` is the operator-facing shortcut for this workflow: continue goal mode from blocking review findings and run the next repair cycle. Use it when you want the guard's state, evidence, or review workflow in a form that is explicit enough to act on immediately.

Continue the Goal repair loop from these blocking findings:

```text
$ARGUMENTS
```

Fix only valid blocking findings, update verification, then stop so the guard can
re-run the review cycle before you claim completion.
