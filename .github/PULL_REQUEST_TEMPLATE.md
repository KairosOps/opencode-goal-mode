<!-- Thanks for contributing! Keep PRs focused. -->

## What & why

<!-- What does this change and why? -->

## Checklist

- [ ] `npm run validate` passes (tests, structure, publish-check, pack)
- [ ] Added/updated a unit test for the behavior change
- [ ] Added a `CHANGELOG.md` entry under a new `## vX.Y.Z` heading
- [ ] Benchmarks still honest (curated sets remain *fixtures*, not accuracy claims)
- [ ] Only `goal` remains user-selectable (other agents stay `mode: subagent`)
